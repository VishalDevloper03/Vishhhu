services:
  - type: web
    name: telegram-bot-host
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python host.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
